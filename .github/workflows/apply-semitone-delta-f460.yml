name: Apply bounded semitone delta parity correction
on:
  push:
    branches: [tuner/64pe-semitone-delta-f460-20260912]
    paths: [.github/workflows/apply-semitone-delta-f460.yml]
permissions:
  contents: write
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo carrier-ok
