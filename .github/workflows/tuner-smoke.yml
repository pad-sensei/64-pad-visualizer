name: TUNER workflow smoke

on:
  push:
    branches:
      - tuner/64pe-semitone-fix-20260912
    paths:
      - .github/workflows/tuner-smoke.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Prove temporary branch workflows execute
        run: |
          echo TUNER_SMOKE_PASS
